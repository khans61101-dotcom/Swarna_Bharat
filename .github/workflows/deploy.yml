name: Deploy Swarna India to cPanel (Automated CI/CD)

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📦 Install Frontend Dependencies & Build React App
        run: |
          cd Front
          npm install --legacy-peer-deps
          npm run build

      - name: 🛠️ Install LFTP
        run: sudo apt-get update && sudo apt-get install -y lftp

      - name: 🚀 Deploy Frontend (React Build) to swarna.hopeharbor.co.in
        run: |
          lftp -c "
            set ftp:ssl-allow no;
            set ftp:passive-mode true;
            open -u '${{ secrets.FTP_USERNAME }}','${{ secrets.FTP_PASSWORD }}' '${{ secrets.FTP_SERVER }}';
            mirror --reverse --delete --verbose ./Front/dist/ ./swarna.hopeharbor.co.in/;
          "

      - name: 🚀 Deploy Backend (Node.js API) to swarnaback.hopeharbor.co.in
        run: |
          lftp -c "
            set ftp:ssl-allow no;
            set ftp:passive-mode true;
            open -u '${{ secrets.FTP_USERNAME }}','${{ secrets.FTP_PASSWORD }}' '${{ secrets.FTP_SERVER }}';
            mirror --reverse --delete --verbose --exclude node_modules/ --exclude .git/ --exclude .env ./Back/ ./swarnaback.hopeharbor.co.in/;
          "
